

const FilterButton = () => {
    
    const filterList = () => {
        console.log("filterList")
    }
    return(
        <button onClick={filterList}>filter</button>
       
    );
}

export default FilterButton